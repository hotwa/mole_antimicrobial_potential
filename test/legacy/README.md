# Legacy Manual Test Scripts

This directory keeps older manual integration scripts that were used before the
current `pytest`/`unittest` coverage stabilized.

These scripts are not part of the default automated test suite. Prefer the
tests under `test/` and the CLI/API smoke checks documented in:

- [`docs/repo_layout.md`](/home/lingyuzeng/project/mole_antimicrobial_potential/docs/repo_layout.md)

